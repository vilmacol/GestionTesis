@model IEnumerable<SistemaBase.Models.Carrera>

@{
    ViewData["Title"] = "Carreras";
}

<div class="mx-n4 px-4 mx-lg-n6 px-lg-6 bg-white pt-6 pb-6 border-y border-300">
    <div class="row align-items-end justify-content-between pb-4 g-3">
        <div class="col-auto">
            <h3 class="mb-2">Carreras</h3>
            <button class="btnAbm" type="button" onclick="openAbmModal('@Url.Action("Create")')"><span class="btn-label"><i class="fa fa-plus"></i></span> Nuevo</button>
        </div>
    </div>

    @if (TempData["Error"] != null)
    {
        <div class="alert alert-danger" role="alert">@TempData["Error"]</div>
    }

    <div class="table-responsive mx-n1 px-1Latest">
        <table class="table table-striped table-bordered">
            <thead>
                <tr>
                    <th class="text-center col-1">Id</th>
                    <th>Descripcion</th>
                    <th>Facultad</th>
                    <th>Estado inicial tesis</th>
                    <th class="text-center col-2"></th>
                </tr>
            </thead>
            <tbody>
                @foreach (var item in Model)
                {
                    <tr>
                        <td class="text-center">@item.IdCarrera</td>
                        <td>@item.Descripcion</td>
                        <td>@item.IdFacultadNavigation?.Codigo</td>
                        <td>@item.IdEstadoInicialTesisNavigation?.Nombre</td>
                        <td class="text-center">
                            <button class="btnAbm" type="button" onclick="openAbmModal('@Url.Action("Edit", new { id = item.IdCarrera })')"><i class="fa fa-pencil"></i></button>
                            <button class="btnEliminar" type="button" onclick="openAbmModal('@Url.Action("Delete", new { id = item.IdCarrera })')"><i class="fa fa-trash"></i></button>
                        </td>
                    </tr>
                }
            </tbody>
        </table>
    </div>
</div>

<div class="modal fade" id="abmModal" tabindex="-1" aria-hidden="true">
    <div class="modal-dialog modal-dialog-centered modal-lg">
        <div class="modal-content" id="abmModalContent"></div>
    </div>
</div>

@section Scripts {
    <script>
        async function openAbmModal(url) {
            const response = await fetch(url);
            document.getElementById('abmModalContent').innerHTML = await response.text();
            new bootstrap.Modal(document.getElementById('abmModal')).show();
        }
    </script>
}
